// Versioned localStorage keys — bump the prefix if the stored format ever changes
// so old values are automatically ignored rather than silently misread.
export const PLAYER_ID_KEY = 'v1:playerId';
export const PLAYER_NAME_KEY = 'v1:playerName';
